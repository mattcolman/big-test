import React, { Component } from 'react';
import CartContent from './CartContent';

// Unfinished component
// ran out of time - but on the up side I show off how CartContent is very reusable :)
class Cart extends Component {
  render() {
    return (
      <div className="Cart">
        <CartContent />
      </div>
    );
  }
}

export default Cart;
